<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>Cappig's blog</title>
    <link href="{{ absolute_url('/blog') }}" rel="alternate"/>
    <link href="{{ absolute_url('/blog/feed.xml') }}" rel="self"/>
    <updated>{{ rfc3339(feed_updated) }}</updated>
    <id>{{ absolute_url('/blog/') }}</id>
    <author>
        <name>Cappig</name>
    </author>

    {% for post in posts %}
    <entry>
        <title>{{ post.title }}</title>
        <link href="{{ absolute_url('/blog/' ~ post.slug) }}" rel="alternate" type="text/html" />
        <id>{{ absolute_url('/blog/' ~ post.slug) }}</id>
        <updated>{{ rfc3339(post.date) }}</updated>
        <summary>{{ post.description }}</summary>
        <content type="html"><![CDATA[{{ post.content | cdata | safe }}]]></content>
    </entry>
    {% endfor %}
</feed>
